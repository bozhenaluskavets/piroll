// setTimeout(() => window.scroll({top: 1000, behavior: 'smooth'}), 3000);

// let homeBloc = document.getElementById('home');
// console.log(homeBloc);
// setTimeout(() => window.scroll({top: homeBloc.scrollHeight, behavior: 'smooth'}), 3000);


// let items = ["url1", "url2"];
// let item = document.getElementById('item');

// let currentUrl = item.image.url;
// let currentUrlIndex = items.indexOf(currentUrl);
// let nextUrlIndex = currentUrlIndex + 1;

// item.image.url = items[nextUrlIndex];

// function mext() {
//     if (currentUrlIndex == (items.length - 1)) {
//         let firstIndex = 0;
//     }
// }

// function prev() {
//     let nextUrlIndex = currentUrlIndex - 1;
    
//     if (currentUrlIndex == 0) {
//         let lastIndex = items.length - 1;
//     }

// }

// FORM VALIDATION

// import { Validators } from "./validators";